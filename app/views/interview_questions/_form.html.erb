<%= form_for(@interview_question) do |f| %>
  <% if @interview_question.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@interview_question.errors.count, "error") %> prohibited this interview_question from being saved:</h2>

      <ul>
      <% @interview_question.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :question_id %><br />
    <%= f.number_field :question_id %>
  </div>
  <div class="field">
    <%= f.label :question %><br />
    <%= f.text_field :question %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
