<%= form_for(@interview) do |f| %>
  <% if @interview.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@interview.errors.count, "error") %> prohibited this interview from being saved:</h2>

      <ul>
      <% @interview.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :application_id %><br />
    <%= f.number_field :application_id %>
  </div>
  <div class="field">
    <%= f.label :date_completed %><br />
    <%= f.date_select :date_completed %>
  </div>
  <div class="field">
    <%= f.label :interview_score %><br />
    <%= f.number_field :interview_score %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
